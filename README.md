# 📘 Final Project — Algorithms & Data Structures

Цей репозиторій містить реалізацію фінального проєкту курсу **Algorithms & Data Structures**.  
Проєкт складається з **7 незалежних завдань**, що демонструють роботу з однозв’язними списками, рекурсією, графами, купами, обходами дерев, жадібними алгоритмами, динамічним програмуванням та методом Монте-Карло.

---

## ⚙️ Вимоги
- Python **3.10+**
- Для візуалізацій використовуються бібліотеки: `matplotlib`, `networkx`

Запуск кожного завдання:
```bash
python task_X_*.py

🧩 Task 1 — Однозв’язний список

Файл: task_1_linked_list.py

Реалізовано:

Реверсування однозв’язного списку шляхом зміни посилань між вузлами

Алгоритм сортування однозв’язного списку

Об’єднання двох відсортованих однозв’язних списків в один відсортований

Результат:

Код виконується коректно, всі операції реалізовані програмно без використання додаткових структур даних.

🌳 Task 2 — Фрактал “Дерево Піфагора”

Файл: task_2_pythagoras_tree.py

Реалізовано:

Рекурсивна побудова фракталу “дерево Піфагора”

Можливість користувача вказати рівень рекурсії

Результат:

Програма коректно візуалізує фрактал для різних рівнів глибини рекурсії.

🗺️ Task 3 — Алгоритм Дейкстри з бінарною купою

Файл: task_3_dijkstra_heap.py

Реалізовано:

Алгоритм Дейкстри для пошуку найкоротших шляхів у графі

Використання бінарної купи (heapq) для оптимізації вибору вершин

Побудова графа та обчислення найкоротших шляхів від початкової вершини до всіх інших

Результат:

Алгоритм працює коректно та має оптимальну часову складність.

📦 Task 4 — Візуалізація бінарної купи

Файл: task_4_heap_visualization.py

Реалізовано:

Побудова та візуалізація бінарної купи

Графічне представлення структури купи

Результат:

Функція коректно візуалізує структуру бінарної купи.

🌲 Task 5 — DFS та BFS (обхід дерева)

Файл: task_5_dfs_bfs_visualization.py

Реалізовано:

Алгоритми DFS та BFS

DFS реалізовано через стек, BFS — через чергу (без рекурсії)

Візуалізація обходу дерева

Колір вузлів змінюється від темного до світлого відповідно до порядку обходу

Результат:

Обидва алгоритми працюють коректно, усі умови завдання виконані.

🍕 Task 6 — Жадібні алгоритми та динамічне програмування

Файл: task_6_greedy_vs_dynamic.py

Реалізовано:

Жадібний алгоритм, що вибирає страви за максимальним співвідношенням калорій до вартості

Алгоритм динамічного програмування (0/1 knapsack) для знаходження оптимального набору страв

Дотримання обмеження за бюджетом

Результат:

Обидві функції коректно працюють та повертають результати відповідно до обраного підходу.

🎲 Task 7 — Метод Монте-Карло (два кубики)

Файл: task_7_monte_carlo_dice.py

Мета:

Змоделювати велику кількість кидків двох ігрових кубиків та визначити ймовірність кожної можливої суми (від 2 до 12).

Реалізація:

Метод Монте-Карло:

Імітація великої кількості кидків

Підрахунок кількості появ кожної суми

Обчислення емпіричних ймовірностей

Аналітичні розрахунки:

Загальна кількість можливих комбінацій — 36

Теоретичні ймовірності для кожної суми

Побудовано таблицю та графік для порівняння результатів

Висновок:

Ймовірності, отримані за допомогою методу Монте-Карло, збігаються з аналітичними значеннями при збільшенні кількості симуляцій. Невеликі відхилення зумовлені випадковим характером методу.